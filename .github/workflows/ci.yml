name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: test_plataforma
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install dependencies
        run: pip install -r backend/requirements.txt

      - name: Lint (ruff)
        run: |
          pip install ruff
          ruff check backend

      # Las dependencias van por rangos (>=5.2,<6), asi que una instalacion
      # nueva puede traer una version distinta de la probada. Sin esto, un CVE
      # publicado entra en el siguiente despliegue sin que nadie se entere.
      # No bloquea el build a proposito: avisa. Convertirlo en bloqueante el dia
      # que un aviso pare un despliegue urgente solo lograria que alguien lo
      # desactive.
      - name: Auditoria de dependencias (pip-audit)
        continue-on-error: true
        run: |
          pip install pip-audit
          pip-audit -r backend/requirements.txt --desc

      - name: Run tests
        working-directory: backend
        env:
          DJANGO_SETTINGS_MODULE: config.settings.local
          POSTGRES_DB: test_plataforma
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_HOST: localhost
          POSTGRES_PORT: "5432"
          DJANGO_SECRET_KEY: ci-only-secret-key-not-for-production
          DJANGO_DEBUG: "True"
          DJANGO_ALLOWED_HOSTS: localhost
        run: python manage.py test --verbosity=2

      - name: Check missing migrations
        working-directory: backend
        env:
          DJANGO_SETTINGS_MODULE: config.settings.local
          POSTGRES_DB: test_plataforma
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_HOST: localhost
          POSTGRES_PORT: "5432"
          DJANGO_SECRET_KEY: ci-only-secret-key-not-for-production
          DJANGO_DEBUG: "True"
          DJANGO_ALLOWED_HOSTS: localhost
        run: python manage.py makemigrations --check --dry-run
